# Laufzeit-Abhängigkeiten. Alles Übrige nutzt die Python-Standardbibliothek.
Pillow>=10.0
numpy>=1.24
imageio-ffmpeg>=0.4.9   # liefert eine statische ffmpeg-Binary mit (kein System-ffmpeg nötig)
yt-dlp>=2024.1          # Link-Input: YouTube-Metadaten + Audio-Download

# Optional (nur für lokale Transkription ohne OpenAI-Key):
# faster-whisper>=1.0

